# Edictum Console — Environment Configuration
# Copy this file to .env and fill in all required values.
#
#   cp .env.example .env
#
# Required secrets can be generated with:
#   python -c "import secrets; print(secrets.token_hex(32))"

# ── Required ──────────────────────────────────────────────

# Postgres password (used by both the postgres container and the server)
POSTGRES_PASSWORD=

# Application DB user password (optional — defaults to POSTGRES_PASSWORD)
# Set this to use a separate password for the edictum app user
# POSTGRES_APP_PASSWORD=

# Redis password (used by the redis container and embedded in REDIS_URL)
# Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
REDIS_PASSWORD=

# Database URL (docker-compose sets this automatically; set for standalone)
# EDICTUM_DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/edictum

# Redis URL (docker-compose sets this automatically; set for standalone)
# EDICTUM_REDIS_URL=redis://:yourpassword@localhost:6379/0

# Secret key for session token signing
# Generate: python -c "import secrets; print(secrets.token_hex(32))"
EDICTUM_SECRET_KEY=

# Ed25519 signing-key encryption secret (32 bytes = 64 hex chars)
# Generate: python -c "import secrets; print(secrets.token_hex(32))"
EDICTUM_SIGNING_KEY_SECRET=

# ── Admin Bootstrap (first run only) ─────────────────────

# Set these to auto-create the admin user on first startup.
# Alternatively, leave blank and use the /dashboard/setup wizard.
EDICTUM_ADMIN_EMAIL=
EDICTUM_ADMIN_PASSWORD=

# ── Optional ──────────────────────────────────────────────

# Public base URL — set to your domain in production (enables Secure cookies)
EDICTUM_BASE_URL=http://localhost:8000

# Auth provider (local is the only option for now)
EDICTUM_AUTH_PROVIDER=local

# Session TTL in hours (default: 24)
# EDICTUM_SESSION_TTL_HOURS=24

# CORS allowed origins (comma-separated)
# EDICTUM_CORS_ORIGINS=http://localhost:8000,http://localhost:3000

# Trusted proxy addresses for X-Forwarded-For/Proto (comma-separated)
# Required when running behind a reverse proxy with HTTPS base URL.
# Use '*' for Railway/Render, or specific IPs/CIDRs for tighter control.
# EDICTUM_TRUSTED_PROXIES=*

# Runtime environment (development | staging | production)
# EDICTUM_ENV_NAME=development
